__pycache__/
*.pyc
*.egg-info/
runs/
outputs/
.pytest_cache/

# uv-managed venv / L2 build trees (not repo state, see env.sh.example)
.venv/
.pixi/
spack/env/.spack-env/
spack/tools/.spack-env/
env.sh

# spack build-run artifacts: machine-specific paths, not reproducible inputs
# (spack.yaml/spack.lock are the reproducible inputs and ARE committed)
spack/env/concretize.txt
spack/env/install.log
spack/tools/concretize.txt
spack/tools/install.log

# 使用者個人資料,不進 repo
diagrams/
papers_lab/
papers_professor/

# RSoft handoff bundles (design geometry - transfer out-of-band, never via git)
rsoft_export/

# 私人筆記(整理給擁有者的 ref/上游草稿),不公開
docs-private/

# Claude worktree admin
.claude/
core.*
